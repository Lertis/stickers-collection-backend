{
  "scripts": {
    "start": "netlify functions:serve",
    "build": "npx tsc",
    "deploy": "netlify deploy --prod",
    "test": "netlify functions:serve"
  },
  "dependencies": {
    "express": "^4.17.1",
    "netlify-cli": "^12.7.2",
    "netlify-lambda": "^2.0.15",
    "serverless-http": "^2.7.0"
  }
}
